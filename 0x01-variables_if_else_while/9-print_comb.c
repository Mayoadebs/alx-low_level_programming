#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Prints a serie of numbers with commas
 *
 * Return: Always (Success);
 */

int main(void)
{
	int d;

	for (d = '0'; d <= '9'; d++)
	{
		putchar(d);
		if (i != '9')
		{
			putchar(',');
			putchar(' ');
		}
	}
	putchar('\n')

	return (0);
}
